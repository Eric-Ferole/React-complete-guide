import React from 'react';

const userOutput = ( props ) => {
  return (
    <div>
      <p>Petits petits {props.name}</p>
      <p>J'aime les petits {props.name}</p>
    </div>
  )
}

export default userOutput;
